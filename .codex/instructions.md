# Інструкції по проєкту (Voxt)

## 📌 Про проєкт
**Voxt** — це Telegram-бот для автоматичної транскрипції голосових повідомлень та відеоповідомлень ("кружечків") у текст. 

## 🛠 Технологічний стек
- **Екосистема:** Node.js, TypeScript
- **Фреймворк:** grammY (для взаємодії з Telegram API)
- **AI / Транскрипція:** OpenAI Whisper API 
- **Зберігання даних:** Локальні JSON-файли (використовується для rate-limiting, без складних баз даних)
- **Інструменти розробки:** `tsx` (для dev-режиму з hot-reload), `dotenv`

## 🎯 Основні принципи
1. **Мінімалізм (KISS та YAGNI):** Мінімальна кількість залежностей, відсутність складної інфраструктури (на кшталт повноцінної БД) для швидкого розгортання та прототипування.
2. **Модульна архітектура:** Чітке розділення відповідальності: 
   - `handlers/` — приймають повідомлення від Telegram.
   - `services/` — містять бізнес-логіку (взаємодія з OpenAI, перевірка лімітів).
3. **Контроль використання:** Вбудований rate-limiting систему (на основі JSON-файлу) для контролю витрат на API.
4. **Гнучкість:** Підтримка різних режимів роботи бота (`auto` або `mention`), а також можливість зміни бекенду транскрипції (OpenAI API або локальний Whisper на Python).
5. **Простий деплой:** Легко запускається локально або на простому VPS за допомогою Node.js.
